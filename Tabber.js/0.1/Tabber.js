// Tabber / 0.1/Tabber.js - <Short Document Title>
//   YourMove Infrastructure (Boilerplate 0.1)
//   Published <Publish date, Day/MM/Year>
//   All rights reserved.

var Tabber = function (Options) {
	// (re)construct options
	var Options = Options || {};
	Options.Placement = Options.Placement || 'Top';
	

	var Container = document.createElement('div');
	Container.class

	// Fork for placement
	if ()
};

// - YourMove Infrastructure